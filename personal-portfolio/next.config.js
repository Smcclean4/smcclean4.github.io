/** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
// };

module.exports = {
  // nextConfig,
  images: {
    domains: ["img.icons8.com"],
    formats: ["image/avif", "image/webp"],
  },
};
